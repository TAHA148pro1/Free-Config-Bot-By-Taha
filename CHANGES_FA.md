# تغییرات این نسخه

- پایش خودکار عضویت کانال‌های اجباری با `chat_member` و بررسی دوره‌ای پشتیبان.
- هنگام خروج کاربر، کانفیگ‌های فعال او فقط با علامت `auto_disabled_by_membership` قطع می‌شوند.
- پیام قطع شامل لینک مستقیم کانال‌های ترک‌شده و دکمه «بررسی عضویت» است.
- پس از تأیید عضویت مجدد، فقط کانفیگ‌هایی که سیستم به‌دلیل عضویت قطع کرده بود دوباره فعال می‌شوند.
- در مدیریت کاربران، دکمه‌های کپی لینک ساب و کانفیگ خام برای ادمین اضافه شد.
- دکمه «کپی کانفیگ» برای کانفیگ‌های بلند به‌جای پیام گمراه‌کننده، کانفیگ را در پیام قابل Copy ارسال می‌کند.
- مدیریت ادمین از داخل ربات: افزودن و حذف ادمین توسط Super Admin.
- Webhook و Polling اکنون `chat_member` update را نیز دریافت می‌کنند.
- Schema migration جدید برای وضعیت پایش عضویت اضافه شد.

پنل VODIWALKER

---

# اصلاحات ربات تلگرام (Root Cause Fixes)

معماری فعلی حفظ شده، هیچ سیستم موازی ساخته نشده و هیچ قابلیتی حذف نشده.
وابستگی جدید، Environment Variable جدید و Migration جدید **لازم نیست**
(`SCHEMA_VERSION` همان ۴ می‌ماند؛ کلیدهای تنظیمات از قبل در
`DEFAULT_BOT_SETTINGS` بودند و `import_state` پیش‌فرض‌ها را Merge می‌کند).

## ۱) Rate Limit ادمین — `botsys/security.py`

**Root Cause:** `middlewares.process` بدون استثنا `security.allow_action()` را
برای همه صدا می‌زد. با پیش‌فرض `rate_limit_per_minute = 20`، ادمین در چند ثانیه
Navigation سریع سقف را پر می‌کرد.

**اصلاح:** استثنا در همان نقطه‌ی مشترک (`allow_action` / `allow_sensitive`)، نه در
Handler ها، تا همه‌ی مصرف‌کننده‌ها یک‌جا درست شوند:

- `rate_limit_exempt()` → Admin و Super Admin از Rate Limit **داخلی** مستثنا.
- `ADMIN_LIMITED_ACTIONS = {"broadcast"}` → فقط ارسال انبوه برای ادمین هم سقف
  دارد (محافظت از خودِ ادمین در برابر 429 رسمی تلگرام).
- Rate Limit کاربران عادی **کاملاً دست‌نخورده** است.

> محدودیت رسمی Telegram API دست نخورده: `tgapi` همان Throttle سراسری و Retry
> روی 429 را دارد.

## ۲) Notification های نامرئی — `botsys/tgapi.py` + `handlers/router.py`

**Root Cause:** Router بی‌درنگ یک Ack **خالی** می‌فرستاد. هر `callback_query`
فقط یک بار قابل پاسخ است، پس همه‌ی `answer_callback` های بعدیِ Handler ها
(«تغییر کرد»، «سهمیه Reset شد»، …) توسط تلگرام دور ریخته می‌شدند و کاربر هیچ
بازخوردی نمی‌دید — که باعث می‌شد Toggle های سالم هم «خراب» به نظر برسند.

**اصلاح:** اول Handler ها پاسخ می‌دهند، Ack خالی فقط در پایان و فقط اگر کسی
پاسخ نداده باشد. دامنه‌ی این وضعیت با `begin_callback` / `end_callback` محدود به
پردازش همان Update است.

## ۳) Toggle ها — `handlers/admin.py` + `keyboards.py`

**Root Cause A (دکمه‌های مرده):** حلقه‌ی مسیریابی پیشوند `a:settings` را *قبل از*
`a:set` بررسی می‌کرد، پس `a:settings:support` و `a:settings:maint` همیشه به
`_settings_section` می‌رفتند؛ آنجا هیچ شرطی برایشان نبود و صفحه فقط دوباره رندر
می‌شد. کدِ واقعیِ Toggle در `_setting_edit_section` **هرگز اجرا نمی‌شد**، پس
مقدار ذخیره نمی‌شد و در Database هم نمی‌نشست.

**Root Cause B (بی‌رنگی):** دکمه‌های Toggle با `k.enter()` ساخته می‌شدند که همیشه
`primary` است، پس وضعیت را نشان نمی‌دادند.

**اصلاح:**
- `TOGGLE_SETTINGS` + `_apply_toggle()`: یک نقطه‌ی مرکزی که Toggle ها را **قبل از**
  مسیریابی پیشوندی اعمال، Audit، Persist و سپس همان صفحه را Refresh می‌کند.
- `keyboards.toggle()` با قابلیت رسمی `style`: **فعال → `success` (سبز)** /
  **غیرفعال → `danger` (قرمز)**. هیچ ایموجی‌ای به‌عنوان جایگزین رنگ نیست.
- پوشش: پشتیبانی، حالت تعمیر، الزام عضویت کانال (هم در Settings و هم در
  Channels، روی یک کلید مشترک)، تمدیدپذیری سهمیه، فعال/غیرفعال هر کانال.
- وضعیت بعد از Restart از State خوانده می‌شود (تست اختصاصی دارد).

## ۴) Navigation و Back — `botsys/nav.py` (جدید)

**Root Cause:** مقصد Back داخل هر Keyboard Builder دستی نوشته می‌شد، پس
صفحه‌های چند-والده Parent اشتباه داشتند.

**اصلاح:** نقشه‌ی اعلانیِ `PARENTS` در یک محل مرکزی + ثبت مسیر ورود برای
صفحه‌های واقعاً چند-والده:

- زنجیره‌ی خواسته‌شده تضمین شد:
  `User Configs → User Details → Users → Admin Panel`
- جزئیات کانفیگ ادمین: قبلاً همیشه به پروفایل کاربر برمی‌گشت، حالا به همان
  لیستی که ادمین از آن وارد شده (`a:usr:*:cfgs:*` یا `a:cfgs:active:*`).
- کانفیگ رایگان: Parent داخل خودِ Callback کدگذاری می‌شود
  (`u:free:menu` / `u:free:cfgs`)، پس بعد از Restart هم درست می‌ماند.
- آموزش اتصال: اگر از جزئیات یک کانفیگ باز شود، Back به همان کانفیگ برمی‌گردد.
- Callback ناشناس/منقضی: با **Edit** روی همان پیام به منو برمی‌گردد، نه پیام جدید.
- نام متغیر محلی `nav` در `admin.py` (که ماژول `nav` را Shadow می‌کرد) به
  `pages` تغییر کرد.

## ۵) Settings — `handlers/admin.py`

- گزینه‌های بدون UI که Backend داشتند اضافه شدند:
  `free_cooldown_seconds`، `broadcast_rate`.
- صفحه‌ی «تنظیمات کانفیگ رایگان» حالا **همه‌ی** کلیدها را نشان می‌دهد
  (سهمیه، حجم، سرعت، مدت، سقف آی‌پی، سقف روزانه/هفتگی/ماهانه، Cooldown) و
  مقدار فعلی روی خود دکمه نوشته می‌شود (Refresh بلافاصله دیده می‌شود).
- **رفع نشت دسترسی:** قبلاً هر ادمینی که `MANAGE_QUOTA` داشت می‌توانست
  `rate_limit_per_minute` را هم عوض کند. حالا `QUOTA_SETTING_KEYS` جدا شده و
  بقیه‌ی کلیدها `MANAGE_SETTINGS` لازم دارند — هم در نمایش فرم و هم در لحظه‌ی
  اعمال مقدار.

## ۶) کانفیگ رایگان و Subscription URL

**Root Cause:** ربات هیچ‌جا لینک ساب نمی‌ساخت. `configs.connection_uri()` فقط
کانفیگ خام (`vless://`) را برمی‌گرداند و `deliver_config` همان را به‌عنوان «لینک
اتصال» در `<code>` و در دکمه‌ی کپی می‌گذاشت.

**اصلاح (استفاده از همان سیستم پنل، بدون سیستم موازی):**
- `main.subscription_url_for_uid()` / `info_url_for_uid()` / `public_base()`:
  تک‌منبع حقیقت. الگوی `/sub/{uid}` قبلاً در **۴ نقطه** دستی ساخته می‌شد؛ همه‌ی
  آن‌ها (شامل `get_link_info` و پرتال‌های وب) حالا همین تابع را صدا می‌زنند.
- `configs.subscription_url()` → دقیقاً همان تابع پنل را صدا می‌زند.
- `configs.raw_config()` → نام گویا برای کانفیگ خام، تا با لینک ساب قاتی نشود.

## ۷) یک پیام واحد + ترتیب دکمه‌ها — `handlers/user.py`

- پیام واسط «⏳ در حال ارسال...» و پیام دوم حاوی لینک حذف شدند؛ نتیجه با
  **Edit** روی همان پیام نوشته می‌شود → **دقیقاً یک پیام**.
- اطلاعات از داده‌ی واقعی `LINKS` پنل: نام، نوع، حجم، **مدت اعتبار**، تاریخ
  انقضا، سرعت، سقف آی‌پی هم‌زمان.
- ترتیب دکمه‌ها ثابت و تغییرناپذیر:
  `کپی لینک ساب` → `کپی کانفیگ` → `کانفیگ‌های من` → `بازگشت`
- «کانفیگ‌های من» تنها دکمه‌ای است که Navigation انجام می‌دهد.

## ۸) دکمه‌های کپی — `keyboards.py`

- از قابلیت رسمی `CopyTextButton` (`copy_text`) استفاده می‌شود: تلگرام خودش متن
  را در Clipboard می‌گذارد و یک Notification کوتاه نشان می‌دهد. **هیچ Callback ای
  فرستاده نمی‌شود**، پس پیام جدید ساخته نمی‌شود، Keyboard عوض نمی‌شود و کاربر
  وارد هیچ منویی نمی‌شود.
- «کپی لینک ساب» = Subscription URL · «کپی کانفیگ» = Raw Config. تست تضمین
  می‌کند این دو هرگز جابه‌جا نشوند.
- **باگ جدی رفع شد:** `copy_btn` قبلاً payload را با `[:256]` می‌برید. سقف رسمی
  `copy_text` همان ۲۵۶ کاراکتر است و یک کانفیگ خام معمولاً بلندتر است، پس کاربر
  یک کانفیگ **خراب** کپی می‌کرد. حالا به‌جای بریدن بی‌صدا، در آن حالت خودِ
  کانفیگ در همان پیام (code block، یک‌ضربه‌کپی) نمایش داده می‌شود و ترتیب و
  تعداد دکمه‌ها عوض نمی‌شود.

## ۹) جلوگیری از Duplicate Config — `services/issuing.py`

روی سه لایه‌ی موجود (`store.claim` + `user_lock` + Cooldown) یک لایه‌ی
**Idempotency** اضافه شد: اگر کانفیگ ساخته شده ولی ارسال تلگرام شکست خورده،
تلاش بعدی **همان** کانفیگ را تحویل می‌دهد (`IssueResult.reused`). بنابراین Retry
هیچ‌وقت کانفیگ دوم، ساب دوم یا مصرف دوباره‌ی سهمیه ایجاد نمی‌کند.

مرتبط: `edit_message` روی «not modified» حالا موفقیت گزارش می‌کند (قبلاً `None`
بود) و `_render` شکست واقعی را منتقل می‌کند، پس هیچ کانفیگی به‌اشتباه
`delivered` علامت نمی‌خورد.

## ۱۰) تست

`tests/test_fixes.py` (جدید، ۲۳ سناریو) به `tests/run_all.py` اضافه شد و هر
مورد بالا را پوشش می‌دهد: کلیک سریع ادمین، Rate Limit کاربر عادی، Edit-only
بودن Navigation، هر پنج Toggle، پایداری بعد از Restart، زنجیره‌ی Back، Parent
چندگانه، کامل بودن Settings، Round-trip هر تنظیم، دسترسی‌ها، منبع لینک ساب،
پیام واحد، ترتیب دکمه‌ها، تفکیک Sub/Raw، کانفیگ بلندتر از ۲۵۶ کاراکتر،
۱۲ کلیک هم‌زمان و Retry بعد از شکست ارسال.

```bash
python3 tests/run_all.py
```

نتیجه: **۲۰/۲۰ + ۲۳/۲۳ + ۹/۹ + ۱۲/۱۲**
